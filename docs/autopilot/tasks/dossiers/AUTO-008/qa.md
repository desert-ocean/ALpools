## Objective

Проверить, что в главном меню остался один пункт `Виртуальный конфигуратор`, он открывает прежний расчётный сценарий, а старый конфигуратор-заглушка удалён из кода.

## Inputs

- `docs/autopilot/tasks/cards/AUTO-008.md`
- `docs/autopilot/tasks/dossiers/AUTO-008/implementation.md`
- `app/handlers/menu.py`
- `app/handlers/cost_handler.py`
- `main.py`

## Decisions

- Считать достаточной локальную верификацию через поиск по коду и синтаксическую проверку затронутых модулей, так как задача текстовая и routing-level.
- Не заводить bug artifact, поскольку дефектов реализации после правки не найдено.

## Work Performed

- Проверен diff по `app/handlers/menu.py`.
- Проверено, что `app/handlers/cost_handler.py` по-прежнему импортирует `BTN_INDIVIDUAL_CALC` и стартует flow по этой константе.
- Проверено, что в `app/handlers/menu.py` отсутствуют `BTN_POOL_TYPE`, кнопка старого конфигуратора и handler заглушки.
- Проверено, что в `app/handlers/menu.py` больше нет stub-handler для `BTN_INDIVIDUAL_CALC`.
- Проверена синтаксическая корректность затронутых файлов командой `python -m py_compile app/handlers/menu.py app/handlers/cost_handler.py main.py`.
- Проверен репозиторный поиск по строкам меню и имени удалённой константы.

## Self-Analysis

- Вывод: для этой задачи основной сигнал качества даёт отсутствие второго handler на тот же текст и сохранение существующего импорта в `cost_handler`.
- Полный runtime UX в Telegram-клиенте не проводился в рамках локальной CLI-проверки.

## Risks

- Остаточные текстовые упоминания слова `configurator` в логах и dossier прошлых задач не влияют на пользовательский сценарий, но существуют как исторический контекст.
- Без запуска бота нельзя подтвердить визуальный порядок кнопок в реальном клиенте, только их описание в коде.

## Handoff

Передать задачу `TeamLead` в статус `ready_for_lead_gate` для финальной проверки артефактов и закрытия.
